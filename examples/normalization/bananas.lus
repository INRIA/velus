node count (i:int) returns (t:int)
let
  t = (0 fby t) + i;
tel

node count_bananas(banana : bool) returns (n : int);
var count : int when banana;
let
  count = count(1 when banana);
  n = merge banana count (n when not banana);
tel;