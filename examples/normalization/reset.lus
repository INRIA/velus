node sum (x : int) returns (y : int)
let
  y = x + (0 fby y);
tel

node reset1 (x : int; r : bool) returns (y : int)
let
  y = (restart sum every r)(x);
tel

node reset2 (x : int) returns (y : int)
let
  y = (restart sum every ((0 fby y) > 42))(x);
tel