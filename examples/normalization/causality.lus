node f (b : int) returns (c : bool; y : int when c);
let
  c = true fby false;
  y = 42 when c;
tel

node use_f (b : int) returns (z : int);
var c : bool; y : int when c;
let
  (c, y) = f(b);
  z = merge c y 0;
tel

node tuple (b : int) returns (x : int; y : int);
let
  (x, y) = (y, b);
tel