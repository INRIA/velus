node f (i : int) returns (x1 : int; x2 : int)
let
  x1 = i + 1;
  x2 = i + 2;
tel
  
node split_test (i : int) returns (x1 : int; x2 : int; x3 : int; x4 : int)
let
  (x1, x2, x3, x4) = (f(i), f(i+1));
tel