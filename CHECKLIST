* Check that TODOs have been removed

->  git grep 'XXX'
->  git grep -i 'TODO'

* Make the tarball

->  make velus.tar.gz

* Check that no unwanted file has jumped in

->  edit .gitattributes to remove them