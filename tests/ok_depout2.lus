
node f(a: bool)
returns (y: bool; z: bool when y)
let
    y = false fby a;
    z = true when y;
tel

node g(a: bool; b: bool when a)
returns (y: bool)
let
  y = true fby false;
tel

node main(b: bool)
returns (x: bool)
let
    x = g(f(b));
tel

