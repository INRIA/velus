#!/bin/sh

SRCFILE=${1:?"Source file not given"}
NODE=${2:-main}
NAME=${SRCFILE%.*}

./velus -dnlustre -dstc -dobc -dclight -sync -main $NODE $SRCFILE && \
CompCert/ccomp -Wl,-emain_sync -stdlib CompCert/runtime -o $NAME.exe $NAME.sync.c $NAME.s
